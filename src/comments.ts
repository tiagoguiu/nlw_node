/**
 * GET => BUSCAR UMA INFORMAÇÃO NA API
 * POST => INSERIR UMA INFORMAÇÃO NA API (ENVIAR)
 * PUT => ALTERAR UMA INFORMAÇÃO
 * DELETE => REMOVER UMA INFORMAÇÃO
 * PATCH => ALTERAR UMA INFORMAÇÃO ESPECIFICA
 */

/**
 * TIPOS DE PARAMETROS
 * Route params => http://localhost:3000/produtos/285419(id)
 * Query params => http://localhost:3000/produtos?name=teclado&description=tecladobom
 * 
 * Body Params =>{
 *      "name" : "teclado",
 *      "description" : "teclado bom"
 * }
 */

/**
 * entity (User) <=> ORM <=> DB
 * repositorio é toda camada que faz a ligação com o nosso banco de dados
 */

/**
 * server -> (controller) -> service ->repositories -> banco de dados
 */